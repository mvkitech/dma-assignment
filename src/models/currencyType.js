// Note: this probably should have been setup to be an enum,
//       but that would have required setting up Typescript.

const CAD = 0;
const USD = 1;
const PESO = 2;

module.exports = { CAD, USD, PESO };
